# Rive Android 渲染器架构指南

## 📋 概述

Rive Android 在 **10.0.0 版本（2025年2月）** 进行了重大架构更新：**完全移除了 Skia 渲染器**，转而使用 Rive 自研的高性能 GPU 渲染引擎。

本文档详细介绍了 Rive 当前的渲染器架构、工作原理以及与之前方案的对比。

---

## 🎨 当前支持的渲染器类型

Rive Android 目前支持两种渲染器：

```kotlin
// RendererType.kt
enum class RendererType(val value: Int) {
    Rive(0),   // 默认：Rive 自研 GPU 渲染器
    Canvas(1); // 备选：Android Canvas 渲染器
}
```

| 渲染器 | 类型 | 说明 |
|--------|------|------|
| **Rive Renderer** | GPU 渲染 | 默认选项，高性能，专为 Rive 动画优化 |
| **Canvas Renderer** | CPU 渲染 | 备选方案，兼容性更好，性能较低 |

---

## 🚀 Rive Renderer（默认渲染器）

### 什么是 Rive Renderer？

**Rive Renderer 是 Rive 公司自主研发的高性能 GPU 渲染引擎**。它：

- 完全独立，不依赖任何第三方图形库（如 Skia）
- 专门针对矢量动画进行了深度优化
- 跨平台统一，确保 iOS、Android、Web 等平台渲染一致性

### 技术架构图

```
┌─────────────────────────────────────────────────────────────┐
│                     Android 应用层                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│                   RiveAnimationView                          │
│                  (Kotlin 高级 API)                           │
│                          │                                   │
│                          ▼                                   │
│                  RiveArtboardRenderer                        │
│                   (渲染器包装类)                              │
│                          │                                   │
│                          ▼                                   │
├─────────────────────────────────────────────────────────────┤
│                     C++ 层 (JNI)                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│                   rive::RiveRenderer                         │
│              (Rive 自研渲染器核心类)                          │
│                          │                                   │
│                          ▼                                   │
│              ┌───────────────────────┐                       │
│              │   RenderContext       │                       │
│              │   (GPU 渲染上下文)     │                       │
│              │                       │                       │
│              │  • Pixel Local Storage│                       │
│              │  • GPU 着色器程序      │                       │
│              │  • 渲染状态管理        │                       │
│              └───────────────────────┘                       │
│                          │                                   │
│                          ▼                                   │
│              ┌───────────────────────┐                       │
│              │   RenderTargetGL      │                       │
│              │   (OpenGL 渲染目标)    │                       │
│              └───────────────────────┘                       │
│                          │                                   │
├─────────────────────────────────────────────────────────────┤
│                   图形 API 层                                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│              ┌───────────────────────┐                       │
│              │     OpenGL ES 3.x     │                       │
│              │   (GPU 图形 API)       │                       │
│              └───────────────────────┘                       │
│                          │                                   │
│                          ▼                                   │
│              ┌───────────────────────┐                       │
│              │         EGL           │                       │
│              │  (OpenGL-Android 桥接) │                       │
│              └───────────────────────┘                       │
│                          │                                   │
├─────────────────────────────────────────────────────────────┤
│                   Android 系统层                             │
├─────────────────────────────────────────────────────────────┤
│                          │                                   │
│                          ▼                                   │
│              ┌───────────────────────┐                       │
│              │    ANativeWindow      │                       │
│              │   (原生窗口表面)       │                       │
│              └───────────────────────┘                       │
│                          │                                   │
│                          ▼                                   │
│              ┌───────────────────────┐                       │
│              │      GPU 硬件         │                       │
│              └───────────────────────┘                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 核心技术详解

#### 1. PLS (Pixel Local Storage)

PLS 是一种 GPU 优化技术，允许在片段着色器中直接访问和修改像素数据，无需通过帧缓冲区。

**优势**：
- 减少内存带宽消耗
- 降低 GPU 功耗
- 提高渲染效率

```cpp
// thread_state_pls.cpp
PLSThreadState::PLSThreadState() {
    // 创建 PLS 渲染上下文
    m_renderContext = rive::gpu::RenderContextGLImpl::MakeContext();
}
```

#### 2. OpenGL ES

Rive Renderer 基于 OpenGL ES 3.x 构建：

```cpp
// worker_impl.cpp
m_renderTarget = rive::make_rcp<rive::gpu::FramebufferRenderTargetGL>(
    width,
    height,
    0,
    sampleCount
);
m_plsRenderer = std::make_unique<rive::RiveRenderer>(renderContext);
```

#### 3. EGL 上下文管理

EGL 负责 OpenGL ES 与 Android 窗口系统的桥接：

```cpp
// thread_state_pls.cpp
eglMakeCurrent(m_display,
               m_backgroundSurface,
               m_backgroundSurface,
               m_context);
```

### 工作流程

```
1. 创建渲染器
   RefWorker::RiveWorker()
   └── 检查 PLS 是否支持
       └── 创建 PLSWorkerImpl
           └── 初始化 RiveRenderer

2. 渲染帧
   doFrame()
   ├── prepareForDraw()    // 准备渲染
   ├── clear()             // 清除画布
   ├── draw()              // 执行绘制（JNI 回调 Kotlin）
   └── flush()             // 提交 GPU 命令

3. 资源清理
   destroy()
   ├── m_plsRenderer.reset()
   └── m_renderTarget.reset()
```

---

## 📱 Canvas Renderer（备选渲染器）

### 什么是 Canvas Renderer？

Canvas Renderer 使用 Android 原生的 Canvas API 进行渲染，是 Rive Renderer 不可用时的备选方案。

### 使用场景

- **Android 模拟器**：某些模拟器不支持 PLS
- **低端设备**：部分老旧 GPU 不支持所需的 OpenGL ES 扩展
- **兼容性需求**：需要最大兼容性的场景

### 自动回退机制

```cpp
// worker_ref.cpp
rcp<RefWorker> RefWorker::RiveWorker() {
    // 检查 PLS 是否支持
    candidateWorker->runAndWait(
        [](rive_android::DrawableThreadState* threadState) {
            auto* plsThreadState = static_cast<PLSThreadState*>(threadState);
            s_isSupported = plsThreadState->renderContext() != nullptr
                                ? RiveRendererSupport::yes
                                : RiveRendererSupport::no;
        });
    
    if (s_isSupported == RiveRendererSupport::yes) {
        // ✅ 使用 Rive Renderer
        s_riveWorker = std::move(candidateWorker);
    } else {
        // ⚠️ 回退到 Canvas
        LOGI("Rive renderer is not supported. Falling back on Canvas.");
    }
}
```

### 手动指定渲染器

```kotlin
// 使用 Builder 指定渲染器类型
val riveView = RiveAnimationView.Builder(context)
    .setRendererType(RendererType.Canvas)  // 强制使用 Canvas
    .build()

// 或在初始化时设置默认渲染器
Rive.init(context, defaultRenderer = RendererType.Canvas)
```

---

## 📊 渲染器对比

### Rive Renderer vs Canvas Renderer

| 特性 | Rive Renderer | Canvas Renderer |
|------|---------------|-----------------|
| **渲染方式** | GPU 硬件加速 | CPU 软件渲染 |
| **性能** | ⭐⭐⭐⭐⭐ 极佳 | ⭐⭐⭐ 中等 |
| **功耗** | 低 | 较高 |
| **兼容性** | 需要 GPU 支持 | 通用 |
| **适用场景** | 生产环境 | 调试/兼容模式 |
| **复杂动画** | 流畅 | 可能卡顿 |

### Skia (已移除) vs Rive Renderer

| 特性 | Skia (已移除) | Rive Renderer (当前) |
|------|--------------|---------------------|
| **来源** | Google 开源库 | Rive 自主研发 |
| **体积** | 较大 (~6MB+) | 较小 |
| **通用性** | 通用 2D 图形引擎 | 专为 Rive 动画优化 |
| **控制权** | 依赖第三方 | 完全自主 |
| **更新频率** | 依赖 Google | 独立快速迭代 |
| **跨平台一致性** | 一般 | 极佳 |

---

## 🔄 版本演进历史

```
版本历史：

v7.0.0 (2023.07)  ─────────────────────────────────
│  PLS Android 集成
│  Rive Renderer 首次引入
│
v8.5.0 (2023.10)  ─────────────────────────────────
│  当 PLS 不支持时回退到 Skia
│
v9.1.0 (2023.12)  ─────────────────────────────────
│  Canvas Renderer 引入
│
v10.0.0 (2025.02) ─────────────────────────────────  ⭐ 里程碑
│  完全移除 Skia
│  仅保留 Rive Renderer + Canvas Renderer
│
当前              ─────────────────────────────────
```

---

## 🔧 移除 Skia 的原因

### 1. 减少包体积

Skia 是一个功能全面的 2D 图形库，包含大量 Rive 不需要的功能：

```
移除 Skia 前后对比：
├── 之前：librive.so + libskia.so ≈ 10MB+
└── 之后：librive.so ≈ 4MB
```

### 2. 性能优化

Rive Renderer 专门针对矢量动画进行了优化：

- **路径渲染**：针对贝塞尔曲线优化
- **混合模式**：高效 GPU 实现
- **遮罩和裁剪**：使用 PLS 技术

### 3. 独立性

不再依赖第三方图形库的更新周期：

- 可以快速修复 bug
- 可以针对 Rive 特性进行优化
- 避免 Skia API 变更带来的兼容性问题

### 4. 跨平台一致性

所有平台使用相同的 Rive 渲染引擎：

- iOS
- Android
- Web (WebGL)
- Desktop

---

## 💡 常见问题

### Q1: Rive 使用 Android 系统的什么组件？

**A**: Rive Renderer 使用以下 Android/系统组件：

| 组件 | 用途 |
|------|------|
| **OpenGL ES** | GPU 图形 API |
| **EGL** | OpenGL 与窗口系统桥接 |
| **ANativeWindow** | 原生窗口表面 |
| **SurfaceView/TextureView** | Android 视图容器 |

但**渲染逻辑完全由 Rive 自研**，不使用 Android 的 Canvas、Skia 等渲染组件。

### Q2: 为什么模拟器上动画可能不流畅？

**A**: 部分模拟器不支持 PLS 扩展，会自动回退到 Canvas Renderer，性能较低。

**解决方案**：
- 使用真机测试
- 使用支持 GPU 加速的模拟器（如 Genymotion）

### Q3: 如何检查当前使用的渲染器类型？

```kotlin
val riveView = findViewById<RiveAnimationView>(R.id.rive_view)

// 渲染器初始化后检查
riveView.artboardRenderer?.let { renderer ->
    Log.d("Rive", "Renderer type: ${renderer.type}")
}
```

### Q4: 什么时候应该手动指定 Canvas Renderer？

- 遇到 GPU 相关崩溃时
- 需要调试渲染问题时
- 目标设备已知不支持 Rive Renderer 时

---

## 📚 相关资源

| 资源 | 链接 |
|------|------|
| Rive 官方文档 | [rive.app/docs](https://rive.app/docs) |
| C++ 运行时源码 | [github.com/rive-app/rive-runtime](https://github.com/rive-app/rive-runtime) |
| Android SDK 源码 | [github.com/rive-app/rive-android](https://github.com/rive-app/rive-android) |

---

## 📝 总结

| 问题 | 答案 |
|------|------|
| **Rive 现在用什么渲染？** | 自研的 Rive Renderer (GPU) |
| **用 Android 系统什么？** | OpenGL ES/EGL，渲染逻辑自研 |
| **有备选方案吗？** | Canvas Renderer (CPU) |
| **Skia 去哪了？** | 10.0.0 版本已完全移除 |

---

**文档版本**：v1.0  
**最后更新**：2024-12-16  
**适用版本**：Rive Android 10.0.0+

