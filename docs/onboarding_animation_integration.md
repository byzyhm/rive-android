# Rive 动画集成文档 - onboarding_part_1.riv

## 一、Rive 核心概念澄清

### 你的理解需要修正的地方

❌ **错误理解**：根据时间节点控制播放"哪个状态机"

✅ **正确理解**：
- 一个 `.riv` 文件通常包含 **一个状态机（State Machine）**
- 状态机有多个 **输入参数（Inputs）**，可以是 Number、Boolean、Trigger 类型
- 你通过 **修改这些输入参数的值** 来触发不同的动画状态/过渡
- 状态机内部会根据输入值的变化自动播放对应的动画

### 概念对照表

| Rive 概念 | 类比 | 作用 |
|-----------|------|------|
| Artboard | 画布 | 包含所有图形元素 |
| State Machine | 动画控制器 | 定义动画逻辑和状态转换 |
| Inputs | 控制变量 | Number/Boolean/Trigger，用于触发状态变化 |
| Text Run | 可变文本 | 可以在运行时动态修改的文本内容 |

---

## 1.1 Inputs 输入类型详解

Rive 状态机的核心是 **Inputs（输入）**，它们是状态机与外部代码通信的桥梁。

### 三种输入类型

| 类型 | 值 | 类比 | 适用场景 |
|------|-----|------|----------|
| **Number** | 浮点数 (0.0, 1.5, 100.0...) | 滑块/旋钮 | 进度、等级、数值变化 |
| **Boolean** | true / false | 开关 | 开/关状态、是否选中 |
| **Trigger** | 无值（只是触发） | 按钮 | 一次性事件、点击 |

### 1.1.1 Number（数值型）

```
┌─────────────────────────────────────────────────────────────┐
│  Number 输入                                                 │
├─────────────────────────────────────────────────────────────┤
│  特点：                                                      │
│  - 持有一个浮点数值                                          │
│  - 值会保持，直到被修改                                       │
│  - 可以用于条件判断（>, <, ==, >=, <=）                       │
│                                                              │
│  工作原理：                                                   │
│  ┌─────────┐    值改变     ┌─────────────────┐               │
│  │ 代码设置 │ ──────────→ │ 状态机检查条件   │               │
│  │ state=1 │              │ state >= 1 ?    │               │
│  └─────────┘              └────────┬────────┘               │
│                                    │ 满足                    │
│                                    ↓                         │
│                           ┌─────────────────┐               │
│                           │ 触发状态转换     │               │
│                           │ 播放对应动画     │               │
│                           └─────────────────┘               │
├─────────────────────────────────────────────────────────────┤
│  代码示例：                                                   │
│  riveView.setNumberState("State Machine 1", "state", 1f)    │
│  riveView.setNumberState("State Machine 1", "progress", 0.5f)│
├─────────────────────────────────────────────────────────────┤
│  使用场景：                                                   │
│  - 进度条动画 (progress: 0.0 → 1.0)                          │
│  - 等级选择 (level: 1, 2, 3)                                 │
│  - 滑块控制 (volume: 0 → 100)                                │
│  - 你的动画中的 state, length 1, length 2, length 3          │
└─────────────────────────────────────────────────────────────┘
```

### 1.1.2 Boolean（布尔型）

```
┌─────────────────────────────────────────────────────────────┐
│  Boolean 输入                                                │
├─────────────────────────────────────────────────────────────┤
│  特点：                                                      │
│  - 只有 true 或 false 两个值                                 │
│  - 值会保持，直到被修改                                       │
│  - 适合二元状态切换                                          │
│                                                              │
│  工作原理：                                                   │
│  ┌─────────┐    true      ┌─────────────────┐               │
│  │ 代码设置 │ ──────────→ │ 状态机检查条件   │               │
│  │ hover=T │              │ hover == true ? │               │
│  └─────────┘              └────────┬────────┘               │
│                                    │ 满足                    │
│                                    ↓                         │
│                           ┌─────────────────┐               │
│                           │ 切换到 Hover 状态│               │
│                           │ 播放悬停动画     │               │
│                           └─────────────────┘               │
│                                                              │
│  ┌─────────┐    false     ┌─────────────────┐               │
│  │ 代码设置 │ ──────────→ │ hover == false ?│               │
│  │ hover=F │              └────────┬────────┘               │
│  └─────────┘                       ↓                         │
│                           ┌─────────────────┐               │
│                           │ 切换回 Normal 状态│              │
│                           └─────────────────┘               │
├─────────────────────────────────────────────────────────────┤
│  代码示例：                                                   │
│  riveView.setBooleanState("State Machine 1", "isHover", true)│
│  riveView.setBooleanState("State Machine 1", "isPlaying", false)│
├─────────────────────────────────────────────────────────────┤
│  使用场景：                                                   │
│  - 悬停效果 (isHover: true/false)                            │
│  - 播放/暂停 (isPlaying: true/false)                         │
│  - 展开/收起 (isExpanded: true/false)                        │
│  - 选中状态 (isSelected: true/false)                         │
│  - 深色/浅色主题 (isDarkMode: true/false)                    │
└─────────────────────────────────────────────────────────────┘
```

### 1.1.3 Trigger（触发型）

```
┌─────────────────────────────────────────────────────────────┐
│  Trigger 输入                                                │
├─────────────────────────────────────────────────────────────┤
│  特点：                                                      │
│  - 没有值，只是一个"信号"                                     │
│  - 触发后自动重置（一次性）                                   │
│  - 类似于按钮点击                                            │
│                                                              │
│  工作原理：                                                   │
│  ┌─────────┐   触发!     ┌─────────────────┐                │
│  │ 代码调用 │ ──────────→ │ 状态机接收信号   │                │
│  │ fire()  │              └────────┬────────┘                │
│  └─────────┘                       │                         │
│                                    ↓                         │
│                           ┌─────────────────┐                │
│                           │ 执行一次状态转换 │                │
│                           │ 播放一次动画     │                │
│                           └────────┬────────┘                │
│                                    │                         │
│                                    ↓ 自动重置                 │
│                           ┌─────────────────┐                │
│                           │ 等待下次触发...  │                │
│                           └─────────────────┘                │
├─────────────────────────────────────────────────────────────┤
│  代码示例：                                                   │
│  riveView.fireState("State Machine 1", "onClick")           │
│  riveView.fireState("State Machine 1", "submit")            │
├─────────────────────────────────────────────────────────────┤
│  使用场景：                                                   │
│  - 按钮点击动画 (onClick)                                    │
│  - 提交/确认 (submit)                                        │
│  - 刷新 (refresh)                                            │
│  - 弹出通知 (showNotification)                               │
│  - 播放一次性特效 (playEffect)                               │
└─────────────────────────────────────────────────────────────┘
```

### 1.1.4 三种类型对比

```
┌──────────────────────────────────────────────────────────────────────┐
│                        Inputs 类型对比                                │
├──────────┬──────────────┬──────────────┬──────────────────────────────┤
│  类型     │ 值            │ 持久性       │ 典型用途                      │
├──────────┼──────────────┼──────────────┼──────────────────────────────┤
│ Number   │ 0.0, 1.5...  │ 保持         │ 进度、等级、连续值            │
│ Boolean  │ true/false   │ 保持         │ 开关、选中、二元状态          │
│ Trigger  │ 无           │ 自动重置     │ 点击、一次性事件              │
└──────────┴──────────────┴──────────────┴──────────────────────────────┘
```

### 1.1.5 状态机工作流程图

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Rive 状态机工作流程                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   Android 代码                    Rive 状态机                        │
│   ───────────                    ────────────                        │
│                                                                      │
│   ┌─────────────────┐                                                │
│   │ 设置 Inputs      │                                               │
│   │ - setNumber     │                                                │
│   │ - setBoolean    │ ──────────────┐                                │
│   │ - fire          │               │                                │
│   └─────────────────┘               │                                │
│                                     ↓                                │
│                          ┌──────────────────────┐                    │
│                          │    状态机检查条件     │                    │
│                          │  (Transitions)       │                    │
│                          └──────────┬───────────┘                    │
│                                     │                                │
│              ┌──────────────────────┼──────────────────────┐         │
│              ↓                      ↓                      ↓         │
│     ┌────────────────┐    ┌────────────────┐    ┌────────────────┐  │
│     │ 条件: state>=1 │    │ 条件: isHover  │    │ 条件: onClick  │  │
│     │ 类型: Number   │    │ 类型: Boolean  │    │ 类型: Trigger  │  │
│     └───────┬────────┘    └───────┬────────┘    └───────┬────────┘  │
│             │ 满足                │ true                │ fired     │
│             ↓                     ↓                     ↓           │
│     ┌────────────────┐    ┌────────────────┐    ┌────────────────┐  │
│     │ State: Blend   │    │ State: Hover   │    │ State: Clicked │  │
│     │ 播放翻译动画    │    │ 播放悬停动画    │    │ 播放点击动画    │  │
│     └────────────────┘    └────────────────┘    └────────────────┘  │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.1.6 你的动画中的 Inputs

根据截图，你的 `onboarding_part_1.riv` 使用的都是 **Number** 类型：

| Input 名称 | 类型 | 作用 | 代码调用 |
|------------|------|------|----------|
| `state` | Number | 控制翻译动画阶段 | `setNumberState("SM", "state", 1f)` |
| `length 1` | Number | 第一条消息的宽度 | `setNumberState("SM", "length 1", 200f)` |
| `length 2` | Number | 第二条消息的宽度 | `setNumberState("SM", "length 2", 300f)` |
| `length 3` | Number | 第三条消息的宽度 | `setNumberState("SM", "length 3", 180f)` |

设计师选择 Number 而不是 Trigger 的原因：
- `state` 需要保持值（state >= 1 时一直播放翻译效果）
- `length` 需要传递具体的数值来控制气泡宽度

---

## 二、从截图中提取的信息（已确认）✅

### 2.1 状态机结构

从 Rive 编辑器截图可以看到：

```
┌─────────────────────────────────────────────────────────┐
│  Artboard: Active                                        │
├─────────────────────────────────────────────────────────┤
│  状态机图层:                                              │
│    bubble1 | text1 | bubble2 | text2 | bubble3 | text3 | bg │
├─────────────────────────────────────────────────────────┤
│  状态流程:                                                │
│                                                          │
│    ┌───────┐      ┌──────────────┐      ┌───────┐       │
│    │ Entry │ ──→ │ popup-Normal │ ──→ │ Blend │       │
│    └───────┘      └──────────────┘      └───────┘       │
│                                                          │
│  - Entry: 入口状态                                        │
│  - popup-Normal: 消息弹出动画                             │
│  - Blend: 翻译过渡动画（混合状态）                         │
└─────────────────────────────────────────────────────────┘
```

### 2.2 Number 输入参数（已确认）

| 变量名 | 类型 | 初始值 | 作用 | 观察到的值范围 |
|--------|------|--------|------|----------------|
| `state` | Number | 0 | 控制动画播放阶段 | 0, 1, 2, 3 |
| `length 1` | Number | 175 | 第一条消息翻译后的内容长度 | 175-204 |
| `length 2` | Number | 138 | 第二条消息翻译后的内容长度 | 138-389 |
| `length 3` | Number | 105 | 第三条消息翻译后的内容长度 | 105-206 |

> ⚠️ **重要发现**：`length` 值范围在 100-400 之间，**应该是像素宽度**，不是字符数！

### 2.3 State 状态流转（已确认）

从截图观察到的动画流程：

```
┌─────────────────────────────────────────────────────────────────┐
│ state = 0 (00:00)                                                │
│ ├── 画面：只有背景渐变 + 加载图标                                  │
│ ├── 状态：popup-Normal                                           │
│ └── 消息气泡尚未出现                                              │
├─────────────────────────────────────────────────────────────────┤
│ state = 0 → popup-Normal 动画播放                                │
│ ├── 三个消息气泡依次弹出                                          │
│ └── 显示多语言原文（法语/韩语/中文）                               │
├─────────────────────────────────────────────────────────────────┤
│ state = 1 (00:10)                                                │
│ ├── 触发 Blend 状态                                              │
│ ├── 第一条消息开始翻译动画                                        │
│ │   "Salut ! Bienvenue à Intent." → "Hi! Welcome to Intent."    │
│ └── 翻译过渡效果：文字逐渐替换                                     │
├─────────────────────────────────────────────────────────────────┤
│ state = 2 (+0.2s)                                                │
│ └── 第二条消息开始翻译动画                                        │
│     "우린 어떤 언어든 읽게 해줘요." → 英文翻译                      │
├─────────────────────────────────────────────────────────────────┤
│ state = 3 (+0.2s) (00:20)                                        │
│ ├── 第三条消息开始翻译动画                                        │
│ │   "而且一眨眼就好！" → "And it works in a blink!"               │
│ └── 最终状态：三条消息都显示英文                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.4 观察到的文本内容

| 消息 | 原文语言 | 原文内容 | 翻译后内容 |
|------|----------|----------|------------|
| 第一条 | 法语 | Salut ! Bienvenue à Intent. | Hi! Welcome to Intent. |
| 第二条 | 韩语 | 우린 어떤 언어든 읽게 해줘요. | We'll make any language readable for you. |
| 第三条 | 中文 | 而且一眨眼就好！ | And it works in a blink! |

---

## 三、自动读取 .riv 文件信息

### 3.1 关于状态机的常见问题

**Q: 一个 .riv 文件只有一个状态机吗？**

❌ **不一定！** 一个 .riv 文件可以包含：
- **多个 Artboard（画板）** - 每个 Artboard 是独立的场景
- **每个 Artboard 可以有多个 State Machine（状态机）** - 控制不同的动画逻辑
- **每个 Artboard 也可以有多个 Animation（时间轴动画）** - 不需要状态机的简单动画

```
.riv 文件结构：
├── Artboard 1 (如 "Active")
│   ├── State Machine 1 (如 "State Machine 1")
│   ├── State Machine 2 (可选)
│   ├── Animation 1 (时间轴动画)
│   └── Animation 2
├── Artboard 2 (可选)
│   └── ...
```

### 3.2 读取 .riv 文件中的状态机名称

你可以用以下代码读取 .riv 文件中的所有信息：

```kotlin
/**
 * 读取并打印 .riv 文件中的所有信息
 * 
 * 将此代码添加到 Activity 中，运行后查看 Logcat
 */
fun printRiveFileInfo(riveView: RiveAnimationView) {
    riveView.post {
        val file = riveView.controller.file ?: run {
            Log.e("Rive", "File not loaded yet!")
            return@post
        }
        
        Log.d("Rive", "========== Rive File Info ==========")
        
        // 获取所有 Artboard
        val artboardCount = file.artboardCount
        Log.d("Rive", "Artboard count: $artboardCount")
        
        // 遍历每个 Artboard
        for (i in 0 until artboardCount) {
            val artboard = file.artboard(i)
            Log.d("Rive", "")
            Log.d("Rive", "--- Artboard [$i]: ${artboard.name} ---")
            
            // 获取状态机名称
            val stateMachineNames = artboard.stateMachineNames
            Log.d("Rive", "State Machines (${stateMachineNames.size}):")
            stateMachineNames.forEachIndexed { index, name ->
                Log.d("Rive", "  [$index] $name")
            }
            
            // 获取动画名称
            val animationNames = artboard.animationNames
            Log.d("Rive", "Animations (${animationNames.size}):")
            animationNames.forEachIndexed { index, name ->
                Log.d("Rive", "  [$index] $name")
            }
        }
        
        // 获取第一个 Artboard 的详细信息（通常是默认使用的）
        val firstArtboard = file.firstArtboard
        Log.d("Rive", "")
        Log.d("Rive", "=== First Artboard Details ===")
        Log.d("Rive", "Name: ${firstArtboard.name}")
        Log.d("Rive", "State Machines: ${firstArtboard.stateMachineNames}")
        Log.d("Rive", "Animations: ${firstArtboard.animationNames}")
        
        Log.d("Rive", "========== End of Info ==========")
    }
}

// 使用示例：在 onCreate 中调用
override fun onCreate(savedInstanceState: Bundle?) {
    super.onCreate(savedInstanceState)
    setContentView(R.layout.activity_onboarding)
    
    val riveView = findViewById<RiveAnimationView>(R.id.rive_animation)
    printRiveFileInfo(riveView)  // 打印文件信息
}
```

### 3.3 预期输出示例

运行上述代码后，Logcat 中会输出类似：

```
D/Rive: ========== Rive File Info ==========
D/Rive: Artboard count: 1
D/Rive: 
D/Rive: --- Artboard [0]: Active ---
D/Rive: State Machines (1):
D/Rive:   [0] State Machine 1
D/Rive: Animations (3):
D/Rive:   [0] popup-Normal
D/Rive:   [1] Blend
D/Rive:   [2] idle
D/Rive: 
D/Rive: === First Artboard Details ===
D/Rive: Name: Active
D/Rive: State Machines: [State Machine 1]
D/Rive: Animations: [popup-Normal, Blend, idle]
D/Rive: ========== End of Info ==========
```

根据输出，你就可以知道状态机的确切名称了！

---

## 四、设计师已确认的信息 ✅

### 4.1 状态机名称

```
StateMachine_1
```

### 4.2 Number 输入参数

| 参数名 | 类型 | 作用 |
|--------|------|------|
| `state` | Number | 控制动画播放阶段 (0, 1, 2, 3) |
| `length_1` | Number | 第一条消息翻译后的内容长度 |
| `length_2` | Number | 第二条消息翻译后的内容长度 |
| `length_3` | Number | 第三条消息翻译后的内容长度 |

### 4.3 Text Run 名称

| Text Run | 作用 |
|----------|------|
| `Content_1` | 第一条消息原文 |
| `Content_2` | 第二条消息原文 |
| `Content_3` | 第三条消息原文 |
| `Translated_1` | 第一条消息翻译结果 |
| `Translated_2` | 第二条消息翻译结果 |
| `Translated_3` | 第三条消息翻译结果 |

### 4.4 动画播放时序

```
时间线:
├── 0s     : state=0, 消息弹出动画开始
├── ~1.5s  : 消息弹出完成，准备翻译
├── 1.5s   : state=1, 第一条消息翻译
├── 1.7s   : state=2, 第二条消息翻译 (+0.2s)
├── 1.9s   : state=3, 第三条消息翻译 (+0.2s)
└── ~3s    : 所有翻译完成
```

---

## 四、设计提供信息的结构化整理

### 4.1 Text Run（可变文本）

| Text Run 名称 | 默认内容 | 目标语言为 ES/KO/ZH-CN 时 |
|---------------|----------|---------------------------|
| `Content 1` | Spanish (法语) | French |
| `Content 2` | Korean (韩语) | Traditional Chinese |
| `Content 3` | Simplified Chinese (中文) | Indonesian |
| `Translated 1` | _翻译结果_ | _翻译结果_ |
| `Translated 2` | _翻译结果_ | _翻译结果_ |
| `Translated 3` | _翻译结果_ | _翻译结果_ |

---

## 五、代码架构设计

### 5.1 推荐架构

```
┌─────────────────────────────────────────────────────────┐
│                    OnboardingActivity                    │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐    │
│  │              RiveAnimationView                   │    │
│  │  (XML 布局中定义，加载 .riv 文件)                 │    │
│  └─────────────────────────────────────────────────┘    │
│                          ↑                               │
│                          │ 控制                          │
│  ┌─────────────────────────────────────────────────┐    │
│  │         OnboardingAnimationController            │    │
│  │  - 封装所有 Rive 交互逻辑                         │    │
│  │  - 管理状态流转                                   │    │
│  │  - 处理文本内容设置                               │    │
│  │  - 计算文本像素宽度                               │    │
│  └─────────────────────────────────────────────────┘    │
│                          ↑                               │
│                          │ 数据                          │
│  ┌─────────────────────────────────────────────────┐    │
│  │              TranslationData                     │    │
│  │  - 原文内容                                       │    │
│  │  - 翻译内容                                       │    │
│  │  - 目标语言                                       │    │
│  └─────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

### 5.2 核心代码实现

#### 数据类

```kotlin
/**
 * 翻译动画数据
 */
data class TranslationData(
    val targetLanguage: String,  // 目标翻译语言代码 (如 "en", "es", "ko", "zh-CN")
    val contents: List<ContentItem>
)

/**
 * 单条消息内容
 */
data class ContentItem(
    val original: String,      // 原文
    val translated: String     // 翻译后的文本
)

/**
 * 需要特殊处理的语言（目标语言是这些时，原文内容需要切换）
 */
val SPECIAL_LANGUAGES = setOf("es", "ko", "zh-CN", "zh-Hans")
```

#### 动画控制器

```kotlin
import android.graphics.Paint
import android.graphics.Typeface
import android.os.Handler
import android.os.Looper
import app.rive.runtime.kotlin.RiveAnimationView

/**
 * Onboarding 动画控制器
 * 
 * 封装所有与 Rive 动画的交互逻辑
 */
class OnboardingAnimationController(
    private val riveView: RiveAnimationView
) {
    // ============ 常量定义 ============
    companion object {
        // TODO: 需要设计师确认状态机名称！
        // 可通过代码获取：riveView.controller.file?.firstArtboard?.stateMachineNames
        private const val STATE_MACHINE_NAME = "State Machine 1"
        
        // Number 输入参数名称（从截图确认）
        private const val INPUT_STATE = "state"
        private const val INPUT_LENGTH_1 = "length 1"
        private const val INPUT_LENGTH_2 = "length 2"
        private const val INPUT_LENGTH_3 = "length 3"
        
        // Text Run 名称（需要设计师确认）
        private const val TEXT_CONTENT_1 = "Content 1"
        private const val TEXT_CONTENT_2 = "Content 2"
        private const val TEXT_CONTENT_3 = "Content 3"
        private const val TEXT_TRANSLATED_1 = "Translated 1"
        private const val TEXT_TRANSLATED_2 = "Translated 2"
        private const val TEXT_TRANSLATED_3 = "Translated 3"
        
        // 翻译动画延迟时间（设计要求 0.2s）
        private const val TRANSLATION_DELAY_MS = 200L
        
        // 用于计算文本像素宽度的字体大小（需要与 Rive 中的字体大小匹配）
        private const val TEXT_SIZE_SP = 16f
    }
    
    private val handler = Handler(Looper.getMainLooper())
    
    // 用于测量文本像素宽度的 Paint
    private val textPaint = Paint().apply {
        textSize = TEXT_SIZE_SP * riveView.resources.displayMetrics.scaledDensity
        typeface = Typeface.DEFAULT  // 需要与 Rive 中使用的字体匹配
    }
    
    /**
     * 初始化动画 - 设置所有文本内容和长度参数
     * 
     * 调用时机：在动画加载完成后、开始播放前调用
     */
    fun initialize(data: TranslationData) {
        // 1. 设置原文内容（根据目标语言选择不同的原文）
        setOriginalContents(data.targetLanguage)
        
        // 2. 设置翻译后的内容
        setTranslatedContents(data.contents)
        
        // 3. 设置长度参数（像素宽度）
        setLengthInputs(data.contents)
        
        // 4. 确保 state = 0（初始状态，播放消息弹出动画）
        setStateValue(0f)
    }
    
    /**
     * 开始播放翻译动画序列
     * 
     * 调用时机：当消息弹出动画完成后，需要开始翻译效果时调用
     */
    fun playTranslationSequence() {
        // 第一条翻译动画
        setStateValue(1f)
        
        // 第二条翻译动画（延迟 0.2s）
        handler.postDelayed({
            setStateValue(2f)
        }, TRANSLATION_DELAY_MS)
        
        // 第三条翻译动画（再延迟 0.2s，累计 0.4s）
        handler.postDelayed({
            setStateValue(3f)
        }, TRANSLATION_DELAY_MS * 2)
    }
    
    /**
     * 重置动画到初始状态
     */
    fun reset() {
        handler.removeCallbacksAndMessages(null)
        setStateValue(0f)
    }
    
    // ============ 私有方法 ============
    
    /**
     * 设置原文内容
     * 
     * 根据设计要求：
     * - 默认情况：Content 1=Spanish, Content 2=Korean, Content 3=Simplified Chinese
     * - 目标语言是 ES/KO/ZH-CN 时：Content 1=French, Content 2=Traditional Chinese, Content 3=Indonesian
     */
    private fun setOriginalContents(targetLanguage: String) {
        if (targetLanguage.lowercase() in SPECIAL_LANGUAGES.map { it.lowercase() }) {
            // 目标语言是 Spanish/Korean/Simplified Chinese，切换原文
            riveView.setTextRunValue(TEXT_CONTENT_1, "Salut ! Bienvenue à Intent.")  // French
            riveView.setTextRunValue(TEXT_CONTENT_2, "我們會讓任何語言都能讀懂。")      // Traditional Chinese
            riveView.setTextRunValue(TEXT_CONTENT_3, "Dan itu bekerja dalam sekejap!") // Indonesian
        } else {
            // 默认情况
            riveView.setTextRunValue(TEXT_CONTENT_1, "¡Hola! Bienvenido a Intent.")   // Spanish
            riveView.setTextRunValue(TEXT_CONTENT_2, "우린 어떤 언어든 읽게 해줘요.")   // Korean
            riveView.setTextRunValue(TEXT_CONTENT_3, "而且一眨眼就好！")               // Simplified Chinese
        }
    }
    
    /**
     * 设置翻译后的内容
     */
    private fun setTranslatedContents(contents: List<ContentItem>) {
        contents.getOrNull(0)?.let {
            riveView.setTextRunValue(TEXT_TRANSLATED_1, it.translated)
        }
        contents.getOrNull(1)?.let {
            riveView.setTextRunValue(TEXT_TRANSLATED_2, it.translated)
        }
        contents.getOrNull(2)?.let {
            riveView.setTextRunValue(TEXT_TRANSLATED_3, it.translated)
        }
    }
    
    /**
     * 设置长度参数
     * 
     * 根据截图观察，length 值在 100-400 范围内，应该是像素宽度
     */
    private fun setLengthInputs(contents: List<ContentItem>) {
        contents.getOrNull(0)?.let {
            val width = measureTextWidth(it.translated)
            riveView.setNumberState(STATE_MACHINE_NAME, INPUT_LENGTH_1, width)
        }
        contents.getOrNull(1)?.let {
            val width = measureTextWidth(it.translated)
            riveView.setNumberState(STATE_MACHINE_NAME, INPUT_LENGTH_2, width)
        }
        contents.getOrNull(2)?.let {
            val width = measureTextWidth(it.translated)
            riveView.setNumberState(STATE_MACHINE_NAME, INPUT_LENGTH_3, width)
        }
    }
    
    /**
     * 测量文本的像素宽度
     * 
     * 注意：这个计算可能需要根据 Rive 中的实际字体和大小进行调整
     */
    private fun measureTextWidth(text: String): Float {
        return textPaint.measureText(text)
    }
    
    /**
     * 设置 state 输入值
     */
    private fun setStateValue(value: Float) {
        riveView.setNumberState(STATE_MACHINE_NAME, INPUT_STATE, value)
    }
    
    /**
     * 释放资源
     */
    fun release() {
        handler.removeCallbacksAndMessages(null)
    }
    
    /**
     * 调试：打印状态机信息
     */
    fun debugPrintStateMachineInfo() {
        riveView.controller.file?.firstArtboard?.let { artboard ->
            println("=== Rive Debug Info ===")
            println("Artboard: ${artboard.name}")
            println("State Machines: ${artboard.stateMachineNames}")
            println("Animations: ${artboard.animationNames}")
        }
    }
}
```

#### Activity 实现

```kotlin
class OnboardingActivity : AppCompatActivity() {
    
    private lateinit var riveView: RiveAnimationView
    private lateinit var animationController: OnboardingAnimationController
    
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_onboarding)
        
        riveView = findViewById(R.id.rive_animation)
        animationController = OnboardingAnimationController(riveView)
        
        // 调试：打印状态机信息（开发时使用）
        riveView.post {
            animationController.debugPrintStateMachineInfo()
        }
        
        // 准备翻译数据（实际项目中应从翻译接口获取）
        val translationData = TranslationData(
            targetLanguage = "en",  // 目标语言
            contents = listOf(
                ContentItem(
                    original = "Salut ! Bienvenue à Intent.",
                    translated = "Hi! Welcome to Intent."
                ),
                ContentItem(
                    original = "우린 어떤 언어든 읽게 해줘요.",
                    translated = "We'll make any language readable for you."
                ),
                ContentItem(
                    original = "而且一眨眼就好！",
                    translated = "And it works in a blink!"
                )
            )
        )
        
        // 初始化动画
        animationController.initialize(translationData)
        
        // 示例：延迟 2 秒后开始播放翻译动画
        // 实际项目中可能需要监听消息弹出动画完成后再触发
        riveView.postDelayed({
            animationController.playTranslationSequence()
        }, 2000)
    }
    
    override fun onDestroy() {
        super.onDestroy()
        animationController.release()
    }
}
```

#### XML 布局

```xml
<?xml version="1.0" encoding="utf-8"?>
<FrameLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <app.rive.runtime.kotlin.RiveAnimationView
        android:id="@+id/rive_animation"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:riveResource="@raw/onboarding_part_1"
        app:riveAutoPlay="true" />
        <!-- 
            注意：如果需要指定状态机名称，添加：
            app:riveStateMachine="状态机名称"
            
            如果不指定，会使用默认状态机
        -->

</FrameLayout>
```

---

## 六、实现步骤清单

### Phase 1: 信息收集 ⏳

- [x] 从截图确认输入参数：`state`, `length 1`, `length 2`, `length 3`
- [x] 确认状态流程：Entry → popup-Normal → Blend
- [x] 确认 `length` 值范围：100-400（像素宽度）
- [ ] 向设计师确认状态机名称
- [ ] 确认 Text Run 的确切名称
- [ ] 确认 `length` 的计算公式（是否需要考虑字体/缩放）

### Phase 2: 基础集成 🔧

- [ ] 将 `onboarding_part_1.riv` 放入 `res/raw/` 目录
- [ ] 创建 XML 布局文件
- [ ] 创建 `OnboardingAnimationController` 类
- [ ] 运行 `debugPrintStateMachineInfo()` 确认状态机名称

### Phase 3: 功能实现 💻

- [ ] 实现文本内容设置逻辑
- [ ] 实现 length 像素宽度计算逻辑
- [ ] 实现状态流转和 0.2s 延迟播放逻辑
- [ ] 处理特殊语言（ES/KO/ZH-CN）的原文切换

### Phase 4: 测试验证 ✅

- [ ] 验证初始动画播放（消息弹出）
- [ ] 验证翻译动画依次播放（带 0.2s 延迟）
- [ ] 验证不同语言的文本切换
- [ ] 验证长文本是否正确显示
- [ ] 验证 RTL 语言（如果需要支持）

---

## 七、常见问题

### Q1: 一个 .riv 文件可以有多个状态机吗？

**可以！** 一个 .riv 文件的结构是：

```
.riv 文件
├── Artboard 1（画板1）
│   ├── State Machine A（状态机A）
│   ├── State Machine B（状态机B）- 可选
│   └── Animations...（时间轴动画）
├── Artboard 2（画板2）- 可选
│   └── ...
```

但在实际项目中，**大多数简单动画只有 1 个 Artboard 和 1 个 State Machine**。

### Q2: 如何获取状态机名称？

参考 [3.2 读取 .riv 文件中的状态机名称](#32-读取-riv-文件中的状态机名称)，运行代码后查看 Logcat。

简化版：

```kotlin
riveView.post {
    val names = riveView.controller.file?.firstArtboard?.stateMachineNames
    Log.d("Rive", "State Machines: $names")
}
```

### Q2: setNumberState 和 setTextRunValue 有什么区别？

| 方法 | 作用 | 示例 |
|------|------|------|
| `setNumberState(stateMachine, input, value)` | 设置状态机的 Number 输入 | `setNumberState("SM", "state", 1f)` |
| `setTextRunValue(runName, text)` | 设置 Text Run 的内容 | `setTextRunValue("Content 1", "Hello")` |

### Q3: 如何监听动画状态变化？

```kotlin
riveView.addEventListener(object : RiveFileController.Listener {
    override fun notifyStateChanged(stateMachineName: String, stateName: String) {
        Log.d("Rive", "State changed to: $stateName")
        // 可以在这里判断动画是否完成
        if (stateName == "Blend") {
            // 进入翻译混合状态
        }
    }
})
```

### Q4: length 参数应该怎么计算？

根据截图观察，length 值在 100-400 范围，应该是**像素宽度**。计算方法：

```kotlin
val paint = Paint().apply {
    textSize = 16f * resources.displayMetrics.scaledDensity
    typeface = Typeface.DEFAULT  // 需要匹配 Rive 中的字体
}
val pixelWidth = paint.measureText("Hi! Welcome to Intent.")
// pixelWidth ≈ 200
```

⚠️ **注意**：可能需要与设计师确认 Rive 中使用的字体和大小，以确保计算一致。

### Q5: 动画播放时序是什么？

```
时间线:
├── 0s     : state=0, 消息弹出动画开始
├── ~2s    : 消息弹出完成，准备翻译
├── 2s     : state=1, 第一条消息翻译
├── 2.2s   : state=2, 第二条消息翻译 (+0.2s)
├── 2.4s   : state=3, 第三条消息翻译 (+0.2s)
└── ~4s    : 所有翻译完成
```

---

## 八、截图信息参考

### 从截图中确认的信息：

| 信息 | 值 | 来源 |
|------|-----|------|
| Artboard 名称 | Active | 截图顶部 |
| 状态流程 | Entry → popup-Normal → Blend | 状态机面板 |
| state 输入范围 | 0, 1, 2, 3 | Inputs 面板 |
| length 1 范围 | 175 - 204 | Inputs 面板 |
| length 2 范围 | 138 - 389 | Inputs 面板 |
| length 3 范围 | 105 - 206 | Inputs 面板 |
| 动画时长 | 20 秒 | 时间轴 |

### 图层结构：

```
bubble1 | text1 | bubble2 | text2 | bubble3 | text3 | bg
```

---

## 九、Rive 字体处理机制

### 9.1 字体的三种处理方式

```
┌─────────────────────────────────────────────────────────────────────┐
│                     Rive 字体处理方式                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  方式1：嵌入字体 (Embedded Fonts)                                    │
│  ────────────────────────────────                                    │
│  - 字体直接打包在 .riv 文件中                                        │
│  - 优点：无需额外配置，开箱即用                                       │
│  - 缺点：.riv 文件体积增大（每个字体 100KB-2MB）                      │
│  - 适用：使用特殊字体、需要保证一致性                                 │
│                                                                      │
│  方式2：外部字体 (Out-of-Band Fonts / OOB)                           │
│  ────────────────────────────────                                    │
│  - 字体不嵌入 .riv，在运行时动态加载                                  │
│  - 优点：多个动画共享同一字体，减少总体积                             │
│  - 缺点：需要额外代码加载字体                                        │
│  - 适用：多个动画使用相同字体、需要优化包大小                         │
│                                                                      │
│  方式3：系统字体回退 (Fallback Fonts)                                 │
│  ────────────────────────────────                                    │
│  - 使用设备系统字体作为后备                                          │
│  - 优点：无需嵌入字体，支持多语言                                     │
│  - 缺点：不同设备显示效果可能不一致                                   │
│  - 适用：需要支持多种语言、CJK 字符                                   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 9.2 检查 .riv 文件是否嵌入了字体

设计师在导出 .riv 文件时，可以选择：
- **嵌入字体**：字体会打包在 .riv 中
- **不嵌入字体**：需要在运行时加载

如果文字不显示，可能是字体没有嵌入。让设计师确认导出设置。

### 9.3 字体 API 详解

Rive Android SDK 提供了多种字体处理方式：

#### 方式 1：直接注册字体（已弃用但仍可用）

```kotlin
import app.rive.runtime.kotlin.core.Rive

// 从 assets 加载字体
val fontBytes = assets.open("fonts/custom_font.ttf").readBytes()
Rive.setFallbackFont(fontBytes)  // 返回 Boolean 表示是否成功

// 从 raw 资源加载
val fontBytes = resources.openRawResource(R.raw.custom_font).readBytes()
Rive.setFallbackFont(fontBytes)

// 使用字体选项（自动从系统字体中匹配）
Rive.setFallbackFont(Fonts.FontOpts(familyName = "sans-serif"))
```

#### 方式 2：FontFallbackStrategy（推荐）

这是官方推荐的方式，支持按字重选择不同字体：

```kotlin
import app.rive.runtime.kotlin.fonts.FontFallbackStrategy
import app.rive.runtime.kotlin.fonts.Fonts
import app.rive.runtime.kotlin.fonts.FontBytes

// 方式 2a：在 Activity 中实现
class OnboardingActivity : AppCompatActivity(), FontFallbackStrategy {
    
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        
        // 设置字体回退策略
        FontFallbackStrategy.stylePicker = this
    }
    
    override fun getFont(weight: Fonts.Weight): List<FontBytes> {
        // 根据字重返回不同字体
        return when {
            weight.weight >= 700 -> {
                // 粗体
                listOf(assets.open("fonts/NotoSansCJK-Bold.otf").readBytes())
            }
            else -> {
                // 常规字重
                listOf(assets.open("fonts/NotoSansCJK-Regular.otf").readBytes())
            }
        }
    }
}

// 方式 2b：全局设置
class MyApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        
        FontFallbackStrategy.stylePicker = object : FontFallbackStrategy {
            override fun getFont(weight: Fonts.Weight): List<FontBytes> {
                // 返回多个字体，按优先级排序
                return listOf(
                    assets.open("fonts/PrimaryFont.ttf").readBytes(),
                    assets.open("fonts/NotoSansCJK.otf").readBytes()  // CJK 回退
                )
            }
        }
    }
}
```

#### 方式 3：使用系统字体

Rive 会自动尝试使用系统字体作为最后的回退，但你也可以手动获取系统字体：

```kotlin
import app.rive.runtime.kotlin.fonts.FontHelper
import app.rive.runtime.kotlin.fonts.Fonts

// 获取系统字体列表
val systemFonts = FontHelper.getSystemFontList()

// 获取特定字体
val fontOpts = Fonts.FontOpts(
    familyName = "sans-serif",  // 或 "serif", "monospace"
    weight = Fonts.Weight.NORMAL,  // 400
    style = Fonts.Font.STYLE_NORMAL  // 或 STYLE_ITALIC
)

// 获取匹配的回退字体
val fallbackFont = FontHelper.getFallbackFont(fontOpts)

// 获取字体文件字节
fallbackFont?.let { font ->
    val fontBytes = FontHelper.getFontBytes(font)
    // 使用 fontBytes...
}

// 一步到位：直接获取回退字体字节
val fontBytes = FontHelper.getFallbackFontBytes(fontOpts)
```

### 9.4 回退字体（多语言支持）

对于需要支持中文、韩文等 CJK 字符的动画，推荐使用 `FontFallbackStrategy`：

```kotlin
// 在 Application 中全局配置
class MyApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        
        // 初始化 Rive
        Rive.init(this)
        
        // 设置支持多语言的回退字体策略
        FontFallbackStrategy.stylePicker = object : FontFallbackStrategy {
            override fun getFont(weight: Fonts.Weight): List<FontBytes> {
                val fonts = mutableListOf<FontBytes>()
                
                // 1. 首选主要字体
                try {
                    fonts.add(assets.open("fonts/primary_font.ttf").readBytes())
                } catch (e: Exception) { }
                
                // 2. CJK 字体（支持中日韩）
                try {
                    fonts.add(assets.open("fonts/NotoSansCJK-Regular.otf").readBytes())
                } catch (e: Exception) { }
                
                // 3. 如果没有自定义字体，使用系统字体
                if (fonts.isEmpty()) {
                    FontHelper.getFallbackFontBytes()?.let { fonts.add(it) }
                }
                
                return fonts
            }
        }
    }
}
```

### 9.5 完整示例：支持中文的 Rive 动画

```kotlin
class OnboardingActivity : AppCompatActivity(), FontFallbackStrategy {
    
    private lateinit var riveView: RiveAnimationView
    
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        
        // 设置字体回退策略（在加载 Rive 之前）
        FontFallbackStrategy.stylePicker = this
        
        setContentView(R.layout.activity_onboarding)
        riveView = findViewById(R.id.rive_view)
        
        // 现在加载的 Rive 动画将使用配置的回退字体
    }
    
    override fun getFont(weight: Fonts.Weight): List<FontBytes> {
        // 优先使用自定义字体，回退到系统字体
        return try {
            listOf(assets.open("fonts/NotoSansSC-Regular.otf").readBytes())
        } catch (e: Exception) {
            // 无法加载自定义字体，尝试系统字体
            FontHelper.getFallbackFontBytes()?.let { listOf(it) } ?: emptyList()
        }
    }
    
    override fun onDestroy() {
        super.onDestroy()
        // 清理策略引用
        if (FontFallbackStrategy.stylePicker === this) {
            FontFallbackStrategy.stylePicker = null
        }
    }
}
```

### 9.6 字体对 APK 大小的影响

| 字体类型 | 大小范围 | 说明 |
|----------|----------|------|
| 英文字体 | 50-200 KB | 只包含拉丁字符 |
| CJK 字体 | 2-20 MB | 包含中日韩字符 |
| 可变字体 | 100KB-5MB | 支持多种粗细 |

**优化建议：**
1. **优先使用系统字体** - 系统已内置 CJK 字体，无需额外打包
2. **多个动画共用同一字体** - 使用 `FontFallbackStrategy` 全局配置
3. **使用字体子集** - 设计师可以在导出时只包含用到的字符
4. **延迟加载** - 仅在需要时加载字体

### 9.7 系统字体回退机制

Rive Android 的字体回退顺序：

```
┌─────────────────────────────────────────────────────────────────────┐
│                     字体回退顺序                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  1. .riv 文件嵌入的字体                                              │
│     ↓ (字符不存在)                                                   │
│  2. FontFallbackStrategy 返回的字体列表（按顺序尝试）                  │
│     ↓ (字符不存在)                                                   │
│  3. Rive.setFallbackFont() 注册的字体                                │
│     ↓ (字符不存在)                                                   │
│  4. 系统字体（自动从 /system/fonts/ 加载）                            │
│     ↓ (字符不存在)                                                   │
│  5. 显示为空白或方块                                                 │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

**重要**：系统字体回退是自动的！如果 .riv 文件使用的字体不支持某个字符（如中文），Rive 会自动尝试从系统字体中查找。

### 9.8 常见字体配置方案

#### 方案 A：完全依赖系统字体（最小 APK）

```kotlin
// 不需要任何配置！Rive 会自动使用系统字体作为回退
// 但效果可能因设备而异
```

#### 方案 B：嵌入主字体 + 系统回退（平衡方案）

```kotlin
// 设计师在 .riv 中嵌入英文字体
// 中文等使用系统字体回退
// 无需额外代码配置
```

#### 方案 C：自定义回退字体（最佳一致性）

```kotlin
// 在 Application 中配置
FontFallbackStrategy.stylePicker = object : FontFallbackStrategy {
    override fun getFont(weight: Fonts.Weight): List<FontBytes> {
        return listOf(
            assets.open("fonts/CustomCJK.otf").readBytes()
        )
    }
}
```

### 9.9 Text Run 不显示的常见原因

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| 文字完全不显示 | 字体未嵌入/未加载 | 让设计师嵌入字体，或加载外部字体 |
| Text Run 找不到 | 名称不匹配 | 确认 Rive 编辑器中的确切名称 |
| Text Run 找不到 | 未导出为动态文本 | 设计师需要在 Rive 中设置为可编辑 |
| 部分字符不显示 | 字体不支持该字符 | 添加回退字体（如 CJK 字体） |

### 9.10 让设计师确认的字体相关信息

请让设计师确认：

1. **字体是否嵌入** - 导出时是否勾选了"Embed fonts"
2. **Text Run 是否可编辑** - 在 Rive 编辑器中，文本需要设置为"Text Run"类型才能在运行时修改
3. **Text Run 的确切名称** - 在 Rive 编辑器中查看

```
Rive 编辑器中设置动态文本：
1. 选择文本元素
2. 在右侧面板找到 "Text Run" 选项
3. 勾选 "Export Name" 并设置名称
4. 这个名称就是代码中使用的名称
```

---

## 十、Text Run 问题排查

### 10.1 当前遇到的问题

从日志可以看到：
```
Failed to set text run 'Content 1': No Rive TextValueRun found with name "Content 1."
```

这说明：
1. ✅ 代码逻辑正确
2. ❌ .riv 文件中没有名为 "Content 1" 的 Text Run

### 10.2 可能的原因

1. **Text Run 名称不匹配** - 设计师提供的名称与实际不同
2. **文本未设置为 Text Run** - 设计师可能忘记将文本设置为可编辑
3. **字体问题** - 但通常字体问题不会导致 "TextValueRun not found" 错误

### 10.3 解决步骤

**步骤 1：让设计师在 Rive 编辑器中确认**

设计师需要：
1. 打开 `onboarding_part_1.riv`
2. 选择每个需要动态修改的文本
3. 在右侧面板查看 "Text Run" 部分
4. 确认 "Export Name" 的确切值

**步骤 2：如果没有设置 Text Run**

设计师需要：
1. 选择文本元素
2. 在右侧面板找到 "Text" → "Runs"
3. 点击 "+" 添加一个 Run
4. 设置 "Export Name"（如 "Content 1"）
5. 重新导出 .riv 文件

---

## 十一、参考资料

- [Rive Android Runtime 官方文档](https://rive.app/docs/runtimes/android/android)
- [State Machines 文档](https://rive.app/docs/runtimes/state-machines)
- [Text 运行时文档](https://rive.app/docs/runtimes/text)
- [Rive Fonts 文档](https://rive.app/docs/runtimes/loading-assets#fonts)
- [Rive Android GitHub](https://github.com/rive-app/rive-android)


